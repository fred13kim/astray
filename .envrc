layout python3

# SETUP arm-none-eabi toolchain

mkdir -p .direnv/bin

for tool in objdump; do
    target=$(command -v arm-none-eabi-$tool)
    if [ -x "$target" ]; then
        ln -sf "$target" .direnv/bin/$tool
    fi
done

export PATH=$PWD/.direnv/bin:$PATH

if [ -f "$PWD/build/app/astray.elf" ]; then
  export ASTRAYOS="$PWD/build/app/astray.elf"
else
  unset ASTRAYOS
fi
